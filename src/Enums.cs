namespace ProjectManager.SDK
{
    public enum DashboardType
    {
        MySummary, 
        PortfolioSummary
    }

    public enum ProjectPermission
    {
        Guest, 
        Collaborate, 
        Editor, 
        Manager
    }
}